<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Aperçu caméra intégré à l'écran Rejoindre (scan de QR, doc 09) — la saisie manuelle du
         code reste toujours disponible, donc pas d'appareil photo requis pour installer/utiliser
         l'app (android:required="false"). -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <!-- Pas d'android:icon pour l'instant : l'icône adaptative (charte §11.5 — couche de fond +
         premier plan, zone sûre 66/108 dp) demande un travail d'asset qui n'est pas fait dans
         cette étape ; icône système par défaut en attendant. -->
    <application
        android:name=".CaCompteApplication"
        android:allowBackup="true"
        android:usesCleartextTraffic="false"
        android:label="Ça Compte"
        android:theme="@android:style/Theme.Material.Light.NoActionBar"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/full_backup_content"
        android:localeConfig="@xml/locale_config">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@android:style/Theme.Material.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
